"""jev-counterspeech: a responsible counterspeech assistant.

Jev (TypeSafe) triages and ranks; a text model only drafts; a human reviews and
"sends" to a local outbox. Nothing is ever posted to a network.
"""

__version__ = "0.1.0"
